{
  "name": "marion-wx-map",
  "version": "1.0.0",
  "description": "Marion Wx Map — Capacitor Android wrapper around the wx-map web app",
  "private": true,
  "scripts": {
    "cap:sync": "npx cap sync android",
    "cap:open": "npx cap open android",
    "android:debug": "cd android && ./gradlew assembleDebug",
    "android:apk": "npm run cap:sync && npm run android:debug && mkdir -p dist && cp android/app/build/outputs/apk/debug/app-debug.apk dist/MarionWxMap-debug.apk && ls -lh dist/MarionWxMap-debug.apk"
  },
  "dependencies": {
    "@capacitor/android": "^6.2.0",
    "@capacitor/core": "^6.2.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.2.0"
  }
}
